Project
1. Create a POST Endpoint.
2. Create an endpoint that can search a product/ product.
3. Create an endpoint that returns a paginated list of products (e.g. first 5, first 10, products
between 5 -10).
4. Create one endpoint that can filter student based on Price Ranges (min and Max price) also
based on price range and product Name e.g. (select all computers between $1000- $3000)